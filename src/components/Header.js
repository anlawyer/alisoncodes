import React, { Component } from "react";

// Placeholder component if I want to add navigation again some day
class Header extends Component {
  render() {
    return <div />;
  }
}

export default Header;
